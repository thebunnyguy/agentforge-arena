# Base URL of the local AgentForge Arena API. If unset, the client falls back
# to window.location.origin (the API/web container serves the built SPA, so
# same-origin is the production default). In dev, vite proxies /api to this.
VITE_API_BASE=http://127.0.0.1:8000
