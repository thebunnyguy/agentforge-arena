{
  "declared_equivalent_mutants": [
    {
      "file": "abatch/batch.py",
      "diff_hash": "sha256:4daa7abbd77072b5e69921516c253cc1a447400be37b2d97dd5a8896669a4d55",
      "reason": "boundary_operator on line 14 (`if batch_size <= 0:`) weakens the guard to `if batch_size < 0:` (LtE -> Lt). This only changes observable behavior for batch_size == 0, since batch_size < 0 is untouched (still raises ValueError('batch_size must be a positive integer') via the explicit check, identical to the reference) and batch_size > 0 never enters either branch. For batch_size == 0, the mutant's explicit check no longer fires, so execution falls through to `factories = list(make_coros)`, `results = []`, and `for start in range(0, len(factories), batch_size):`. Python's range() constructor unconditionally rejects a step of 0 -- verified empirically that range(0, 0, 0) raises ValueError('range() arg 3 must not be zero') immediately, exactly like range(0, N, 0) for any N -- so EVERY call with batch_size == 0 still deterministically raises ValueError, just from range()'s own step-validation instead of the explicit guard, and with a different (contract-unspecified) message. Reproduced by hand (mutant copy, not the committed reference) with call-counting factories: run_in_batches([], 0) raises ValueError; run_in_batches([f1, f2], 0) also raises ValueError with zero factories ever invoked (list(make_coros) on a list is a pure O(n) copy -- it does not call any element); run_in_batches(<a 3-item generator>, 0) also raises ValueError, and while the generator IS fully consumed by list() before the error (an unpromised side effect the docstring never rules out either way -- it says nothing about whether make_coros is touched on an invalid batch_size), none of the yielded factories are ever called and no coroutine is ever created via asyncio.gather in any case. So for every input the task contract cares about (make_coros as documented, any batch_size), the mutant produces: the same exception TYPE (ValueError), the same non-execution of every factory/coroutine, and the same empty result (no partial results returned) as the reference -- only an internal, contract-unspecified error message and the (side-effect-free, for actual factories) point at which make_coros is read differ. This is also not an oracle weakness for the batch_size==0 case in general: test_nonpositive_batch_size_rejected[0] still correctly kills any implementation that does NOT get a free ValueError from range() (e.g. a hand-rolled `while`/index loop with a weakened boundary check just returns [] silently for empty input, or hangs/misbehaves for non-empty input) -- this particular survival is specific to the reference's own range()-based loop shape providing a redundant safety net, not a gap in what the hidden suite enforces in general."
    }
  ]
}
