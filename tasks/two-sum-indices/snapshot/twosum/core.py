def two_sum(nums, target):
    """Return the indices of two distinct elements of ``nums`` that sum to ``target``.

    ``nums`` is a sequence of integers. Return a tuple ``(i, j)`` with ``i < j``
    such that ``nums[i] + nums[j] == target``. If no such pair exists, return
    ``None``. There is at most one valid answer.

    This must be EFFICIENT: an O(n) single pass using a hash map of
    previously-seen values, NOT an O(n^2) double loop. A quadratic solution is
    cut off by the grader's timeout on the large hidden input.

    STUB: not implemented yet.
    """
    raise NotImplementedError("two_sum is not implemented yet")
