{
  "declared_equivalent_mutants": [
    {
      "file": "twosum/core.py",
      "diff_hash": "sha256:f0dcac4e27e150f1ff1befac697ef634573881e252ef4ec5945d171542f47fa6",
      "reason": "remove_branch_force_true at core.py:21:8 changes `if value not in seen: seen[value] = j` into an unconditional `seen[value] = j` on every iteration, so `seen[value]` always holds the index of the MOST RECENT occurrence of `value` seen so far, instead of the reference's documented 'only record the first occurrence so the smallest index is kept'. This can only change the returned tuple if, at the moment some later index j looks up `seen.get(target - nums[j])`, the complement value has appeared more than once at indices i_1 < i_2 < ... < i_k < j: the reference returns (i_1, j) (earliest), the mutant returns (i_k, j) (most recent). But if nums[i_1] == nums[i_2] == ... == complement == target - nums[j], then BOTH (i_1, j) and (i_2, j) independently satisfy nums[i]+nums[j]==target with i<j -- i.e. at least two distinct valid answers exist for that input. The task's own written contract (task.json description: 'two distinct indices whose values sum to target ... (at most one valid answer)') explicitly guarantees the input never has more than one valid index pair. Under that guarantee, k<=1 always holds at the point of any successful lookup (no value that participates in the eventual answer can have more than one prior occurrence), so i_1 == i_k trivially and the mutant is byte-for-byte identical to the reference on every value it returns. Divergence is provably impossible for any input the contract admits; it can only be produced by feeding an input that already violates 'at most one valid answer' (e.g. nums=[3,3,5], target=8, which has two valid answers (0,2) and (1,2) even before considering this mutant), which is out of the domain the hidden/regression suites are entitled to construct. Verified empirically: 200000 randomized trials (small-vocabulary integer arrays engineered to produce duplicates frequently) found zero divergences between the reference and the reconstructed mutant across all 157566 generated inputs satisfying the <=1-valid-answer contract, while 11524 of the 42434 inputs that violated the contract (out of scope) did diverge -- exactly the boundary the proof predicts. The existing hidden test `test_duplicates_resolved_correctly` (nums=[3,1,2,3], target=6) does not kill this mutant precisely because the single repeated value (3) has only one occurrence before its own matching index, consistent with the contract; it was never possible to kill this survivor without accepting an ill-formed input outside the task's stated guarantee, which the mission's own rule against pattern-matched/mutant-tailored hidden tests forbids doing anyway."
    }
  ]
}
