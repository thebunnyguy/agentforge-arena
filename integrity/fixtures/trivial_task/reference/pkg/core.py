def double(n):
    """Return n doubled. (Textually different from the snapshot — adds this
    docstring — so the reference overlay is a non-empty diff, while remaining
    behaviorally identical to the already-correct, mistakenly-shipped
    snapshot this fixture is testing for.)"""
    return n * 2
